{{- if .Values.openshift.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rocketgraph.fullname.serviceaccount" . }}
  labels:
    {{- include "rocketgraph.labels" . | nindent 4 }}
{{- end }}
